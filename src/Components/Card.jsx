import React from 'react'
import '../App.css'


// function Card() = ({ name, country, weather.main, weather.description, main.temp, main.feels_like, main.pressure, main.humidity, coord.lon, coord.lat, timezone}) => 

function Card() {
  return (
    <div className='card-container'>
      <h2></h2>
    </div>
  )
}

export default Card